Although people commonly say "Docker image", the format itself is no longer Docker-specific. Docker, Podman, containerd, CRI-O, and many other container runtimes all support the **OCI Image Specification**.

